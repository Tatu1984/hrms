/**
 * Common Components
 * Re-exports shared components and new common utilities
 */

// Re-export shared components - use named exports where default doesn't exist
export { default as Navbar } from '../shared/navbar';
export { default as Sidebar } from '../shared/sidebar';
export * from '../shared/BrowserActivityTracker';

// New common components will be added here as the codebase evolves
