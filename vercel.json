{
  "buildCommand": "npx prisma db push --accept-data-loss --skip-generate && npx prisma generate && next build",
  "crons": [
    {
      "path": "/api/attendance/auto-heartbeat",
      "schedule": "*/3 * * * *"
    }
  ]
}
